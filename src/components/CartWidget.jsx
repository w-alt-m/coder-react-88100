const CartWidget = () => {
  return (
    <div>
      {/* debe mostrar un icono */}
      <span>🛒</span>
      <span style={{ color: "red", fontWeight: "bold" }}>5</span>
    </div>
  );
};

export default CartWidget;
